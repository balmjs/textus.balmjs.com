# SPA fallback for all non-API routes
# Cloudflare Pages Functions (functions/api/[[path]].ts) handles /api routes with highest priority
# The condition below ensures only non-API and non-static paths are redirected to index.html
/* /index.html 200
