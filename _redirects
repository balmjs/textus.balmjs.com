# Cloudflare Pages routing rules
# Functions in /functions directory handle /api/* routes automatically
# This file provides SPA fallback for frontend routes only

# Exclude API routes from SPA fallback (handled by Functions)
/api/*  /api/:splat  200

# SPA fallback for all other routes
/*  /index.html  200
